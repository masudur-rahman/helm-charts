.idea

# Plaintext / decrypted secrets — NEVER commit. Only *.sops.yaml (encrypted) is tracked.
porkbun-credentials.yaml
porkbun-credentials.yml
*.dec.yaml
*.dec.yml
secret.yaml
secret.yml

# …but Helm chart Secret *templates* are source, not decrypted secrets — keep them.
!charts/**/templates/secret.yaml
!charts/**/templates/secret.yml

# age private keys
*.agekey
age.key
key.txt
