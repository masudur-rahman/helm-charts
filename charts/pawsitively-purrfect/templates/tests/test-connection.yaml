apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pawsitively-purrfect.fullname" . }}-test-connection"
  labels:
    {{- include "pawsitively-purrfect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pawsitively-purrfect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
