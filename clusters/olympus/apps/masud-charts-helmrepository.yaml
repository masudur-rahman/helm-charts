# Published Helm repo (GitHub Pages custom domain, stable/ index) for
# masudur-rahman's charts. Shared source for any HelmRelease pulling from it.
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: masud-charts
  namespace: flux-system
spec:
  interval: 1h
  url: https://charts.mrahman.xyz/stable
